{
  "name": "querypie-worker",
  "version": "1.0.0",
  "description": "QueryPie Playwright extraction worker — 사내망 Mac에서 실행",
  "scripts": {
    "start": "node server.js",
    "install-browser": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.50.0",
    "unzipper": "^0.12.3"
  }
}
